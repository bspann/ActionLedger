namespace ActionLedger.Infrastructure;

/// <summary>
/// Compile-time handle on the Infrastructure assembly. The EF Core context, repositories,
/// AI adapters, and the outbox pipeline arrive with Epic 1 Story 1.3 onward.
/// </summary>
public sealed class InfrastructureAssemblyMarker;
