namespace ActionLedger.Api.Configuration;

/// <summary>Environment names the host behaves differently in.</summary>
public static class ApiEnvironments
{
    /// <summary>The name compose sets. Swagger UI is served here as well as in Development (AD-13).</summary>
    public const string Compose = "Compose";
}
