## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Issue

<!-- Required. The `require-linked-issue` check fails without one. -->
Closes #

## Checklist

- [ ] **Issue link** — this pull request links its issue on the Project board
- [ ] **Tests added** — the change is covered in the test project for its ring (AD-18), and `dotnet test` is green locally
- [ ] **License review** — every new or bumped dependency is MIT, Apache-2.0, or BSD, pinned in `Directory.Packages.props`, and none of the excluded packages (FluentAssertions 8+, MediatR 13+, AutoMapper 15+, MassTransit 9, JsonSchema.Net)
- [ ] **axe pass** — UI stories only: an axe scan of the changed screens reports no violations; indicators are icon plus text, icon buttons have accessible labels, fields have visible labels

## Notes for the reviewer

<!-- Anything worth knowing: a decision you made, something you deliberately left out,
     a follow-up you filed. Delete if there is nothing. -->
