---
scope: ActionLedger v1.0.0 whole system
purpose: build substrate for a 3-day single-developer build plus interview walkthrough; ADRs are a demo deliverable
altitude: initiative to features
updated: 2026-09-20T11:51
---

- (event) Create intent, fast path, autonomous. Inputs: PRD + addendum (final), brief addendum (locked stack, layering, 7 ADR list, build sequence), UX spines (final), seed docs/bmad-seed-prompt.md. Purpose: build substrate for Amelia plus a human-facing architecture document and seven ADR files as interview deliverables under docs/. Audience: one developer with Claude Code, then the interview panel
- (constraint) Seed locks: C#/.NET 10 ASP.NET Core Web API controllers; EF Core code-first PostgreSQL with migration bundle; Angular current stable, strict TS, standalone, signals, Material; Microsoft.Extensions.AI with providers AzureOpenAI, local, Fake; xUnit, Testcontainers, NetArchTest, Angular unit tests, one Playwright smoke; docker compose; permissive licenses only; plain handler classes, no mediator
- (constraint) Seed locks Clean Architecture: Domain <- Application <- Infrastructure, Api composition root; Domain and Application reference no EF Core, ASP.NET Core, or AI SDK; NetArchTest enforces; approval transitions are entity logic; provider swap = config + one Infrastructure class
- (constraint) PRD resolutions inherited: LocalOpenAI provider (LM Studio primary, Ollama by base URL) replaces the seed's named Ollama; JWT symmetric key, 8h, PasswordHasher; outbox worker recommended as BackgroundService in API; eval gate deterministic matcher; thresholds in FR-39
- (decision) Paradigm: Clean Architecture (onion) with explicit use-case handler classes; one handler per use case in Application, invoked directly by controllers; no mediator. Binds all backend units. Prevents controllers growing domain logic and handlers depending on HTTP
- (decision) Domain model: aggregates are Meeting (owns MeetingNotes, ExtractionRuns), ExtractionRun (owns ProposedActions), TrackedAction (owns its ActionRevisions), plus User, WebhookSubscription, OutboxMessage as standalone. ProposedAction.Decide(...) and TrackedAction.Transition(...) are the only mutation paths for Review State and Action Status; they raise domain events. Prevents two writers of state
- (decision) Persistence: single DbContext in Infrastructure; repositories per aggregate defined as Application interfaces; unit of work = DbContext.SaveChanges via IUnitOfWork; outbox rows written in the same SaveChanges as the aggregate change (transactional outbox). Domain events dispatched in-process inside SaveChanges pipeline before commit so OutboxMessage rows are part of the same transaction
- (decision) Audit: explicit ActionRevision table (append-only), written by the aggregate methods themselves, not by an interceptor; AI Proposal row written when the ExtractionRun stores proposals. Alternatives temporal tables (no attribution of actor/kind) and event sourcing (rebuild complexity, 3 days) rejected
- (decision) AI: IActionExtractor in Application takes (notesText, meetingDate, promptVersion) and returns a validated ExtractionResult; Infrastructure has one ChatClientActionExtractor built on Microsoft.Extensions.AI IChatClient, and three IChatClient factories (AzureOpenAI, LocalOpenAI via OpenAI SDK with custom endpoint, Fake). Schema validation with JsonSchema.Net in Infrastructure; retry-once policy in the extractor. Provider selected by config key Ai:Provider
- (decision) Prompt files under /prompts embedded as content files copied to output; version parsed from filename; PromptCatalog in Infrastructure
- (decision) Webhooks: transactional outbox; OutboxDispatcher BackgroundService in the Api process polls with FOR UPDATE SKIP LOCKED every 5s, claims up to 20, delivers via HttpClient with HMAC-SHA256 over timestamp.body; retry schedule from PRD FR-32; separate container documented as scale path
- (decision) Auth: ASP.NET Core JwtBearer with symmetric key from config; login endpoint verifies PasswordHasher<User>; ICurrentUser reads sub claim via IHttpContextAccessor adapter in Infrastructure; roles via ClaimTypes.Role and [Authorize(Roles=...)]
- (decision) API: /api/v1 prefix, ProblemDetails for errors with stable type URIs, OpenAPI via Microsoft.AspNetCore.OpenApi + Swagger UI; Angular client generated from openapi.json in the web build. DTOs live in Application; controllers map only
- (decision) Frontend: Angular feature folders meetings, review, actions, audit, auth; smart container per route, presentational children; MVVM = template/view, component class/viewmodel with signals, generated API client/model; HTTP only in feature data services wrapping the generated client
- (decision) Deployment: docker compose with services db (postgres), api, web (nginx serving Angular build, proxying /api), receiver (tiny webhook echo); migration bundle run as a one-shot compose service before api; CD to Azure Container Apps with the same images; LM Studio reached from containers at host.docker.internal:1234/v1
- (decision) Testing: Domain.Tests pure; Application.Tests with Fake provider and in-memory repos; Infrastructure.Tests with Testcontainers PostgreSQL; Api.Tests with WebApplicationFactory; Architecture.Tests NetArchTest; Eval project is a console + xunit test that runs the scorer; Playwright smoke in tests/Web.E2E
- (question) Versions pending web verification: .NET 10 patch, EF Core 10, Npgsql, Microsoft.Extensions.AI, OpenAI SDK custom endpoint support, LM Studio structured output, Angular major, Material, Testcontainers, NetArchTest maintenance, xUnit v3, Playwright, JsonSchema.Net license
- (event) Drafted ARCHITECTURE-SPINE.md (19 ADs, conventions, stack pending version check, 3 seed diagrams, capability map, deferred), ARCHITECTURE.md (context, layers, provider swap proof, ERD with attributes, extraction+approval and outbox sequence diagrams, identity, MVVM table, deployment matrix, testing matrix, ADR index), and adrs/ADR-001..007 with alternatives
- (assumption) Versions in the Stack table are drafted from training knowledge and will be corrected from the version-research subagent before the reviewer gate
- (version) .NET 10.0.12 LTS; ASP.NET Core OpenApi 10.0.12; Swashbuckle.AspNetCore.SwaggerUI 10.2.3 MIT; EF Core 10.0.12; Npgsql EF 10.0.3; PostgreSQL 18.6 (postgres:18-alpine); M.E.AI + M.E.AI.OpenAI 10.10.0; OpenAI SDK 2.14.0 (OpenAIClientOptions.Endpoint); Azure.AI.OpenAI 2.1.0 stable; xunit.v3 4.0.1; Testcontainers.PostgreSql 4.15.0; NetArchTest.eNhancedEdition 1.4.5; Playwright 1.62.0; Angular + Material 22.1.7; LM Studio 0.4.25 with json_schema response_format; ACA deploy action v2. Sources: dotnet.microsoft.com, nuget.org, learn.microsoft.com, lmstudio.ai/docs, angular.dev, docker-library postgres README
- (decision) Dropped JsonSchema.Net (json-everything): NuGet binaries carry an Open Source Maintenance Fee EULA since 2026-02, which fails the seed's permissive-license rule. Replacement: strict System.Text.Json deserialization into ExtractionOutput + explicit ExtractionOutputValidator; schema file remains the contract via a JsonSchemaExporter equivalence test. Binds AD-11. Prevents a validation path that differs from the schema file
- (decision) NetArchTest.Rules is unmaintained since 2021; use NetArchTest.eNhancedEdition 1.4.5 (MIT). Same fluent API, satisfies the seed's 'NetArchTest' intent
- (question) Azure.AI.OpenAI stable 2.1.0 pairing with Microsoft.Extensions.AI.OpenAI 10.10.0 is unverified. Azure is optional in v1; fallback is OpenAI SDK against Azure's OpenAI-compatible endpoint. Revisit if/when the Azure factory is built
- (assumption) Node 22 LTS and TypeScript 5.9 are not web-verified; Angular 22's supported Node range should be checked at scaffold time
- (decision) Applied reconcile-inputs (about 40 items): ActionRevision is a standalone entity emitted by three aggregates (AD-3/AD-7 reconciled); ports enumerated incl. seed's IActionRepository and IWebhookDispatcher; AD-1 denylist + allowed abstractions; AD-8 carries the full header/signature contract, per-attempt timestamp, payload enrichment in the outbox pipeline, masked secret, receiver verification, docs/webhooks.md; AD-9 server computes Approve vs Edited from diff, reason in body; AD-11 Ai:CallTimeoutSeconds, schema file versioned, SourceExcerptFilter shared with Eval, Fake keyed by notes SHA-256 with embedded seed fixtures, GET ai/provider, isLowConfidence; AD-13 resource list incl. users and health, tracked-actions query params, load-bearing DTO fields, committed openapi.json with CI diff check, Swagger envs, error type set fixed; AD-15 derived flags server-side; AD-16 startup probe, API_UPSTREAM; AD-17 postgres:18-alpine, seeder in api after migrate, nginx 200s timeouts, Dockerfiles, no-op deploy path, codeql/dependabot; AD-19 md summary, server field, optional Azure; conventions add correlation id, licensing, indexes, branch protection; tree adds docs/, .github files; capability map adds NFR-2, NFR-5, docs deliverables
- (version) review-versions corrections applied: TypeScript 6.0.3 (Angular 22 needs >=6.0 <6.1), Serilog.AspNetCore 10.0.0, Node 24.21.0 Active LTS (revisit 2026-10-20), nginx 1.30.5, Ollama 0.34.2, .NET SDK 10.0.401; Azure.AI.OpenAI dropped in favor of OpenAI SDK against Azure /openai/v1 endpoint (Microsoft's documented path); strict=true and schema-transform behavior recorded in AD-11; JsonSchemaExporter claim narrowed to names/required/types; extra_hosts host-gateway added to api service
- (decision) AD-20 added (rubric H-1 + adversary F-2): one commit per use case via IUnitOfWork.CommitAsync called once by the handler; xmin concurrency tokens on ProposedAction, TrackedAction, Meeting, OutboxMessage; unique indexes on tracked_action(proposed_action_id), meeting_notes(meeting_id), proposed_action(run, ordinal), user(username), meeting(title, date); concurrency and unique violations map to 409; Api.Tests races two decisions. Prevents two officers deciding one proposal and partial writes across aggregate, revision, outbox
- (decision) AD-21 added (adversary F-5/F-7, rubric H-3): fixtures/extraction/ is the single catalog for Golden Set, Fake answers (keyed by SHA-256 of normalized notes), and seed source; seeder is a hosted service in api before the dispatcher, Seed:Enabled, advisory lock, aggregates with SeedCurrentUser + FixedClock, direct writes only for Delivered/Dead outbox rows, SaveChangesAsync(suppressOutbox) restricted to Infrastructure/Seed by architecture test. Rejected: seeding in the migrate container (bundle cannot run C#)
- (decision) Reviewer fixes applied: ActionRevision is its own root with Sequence and a single now per aggregate call (F-1, H-2); events raised on the returned TrackedAction root and handler must Add (F-4); WebhookEventDto + WebhookEventTypes + IWebhookPayloadBuilder in Application, IClock in DbContext, shared EventId, lease semantics (F-3, F-16, M-2); OwnerResolver pure fn used by ProposedActionReadModel and the change test only, IExtractionSettings port (F-6); ExtractAsync never throws, Failed run is 201 (F-8, M-1); Overdue as Domain expression (F-9); committed openapi.json + OpenApiSnapshotTest + ng-openapi-gen, no .NET in web image (F-10, C-1); TextNormalization/ExcerptVerifier in Application/Ai, SchemaVersion from schema version property (F-11, H-4, M-3); ValueGeneratedNever + Add-only (F-12); ExtractionRun is a root created by Start (F-13); users endpoint + users store (F-14); prompts as embedded resources + Ai:PromptVersion (F-15); decision copy on proposal asserted equal to revision (F-17); Timeouts, Logging/correlation, Pipeline, API contract rows (H-5, H-6, M-6); registry GHCR, tags, health paths, Azure DB Flexible Server, secret source (H-6); Angular unit tests in AD-18, NFR rows in capability map (M-5); ERD cardinalities, host naming, jsonb/text[] converters, FR-40 wording, ESLint rule, startup probe (L-1..L-7); ADR-007 gains step 5 rowversion/sp_getapplock
- (event) Triage: no blocking open items. Open question kept in memlog: Azure /openai/v1 path adopted per Microsoft docs, pairing to be smoke-tested if Azure factory is built. Deferred list extended with backup/restore, migration rollback, staging. Polish: bmad-review structure,prose on ARCHITECTURE.md and adrs/ (not the spine) via parallel subagents
- (event) spine finalized: 21 ADs, lint clean, reviewer gate (rubric, adversary, versions) and input reconciliation applied, ARCHITECTURE.md (20 polish edits, mermaid verified) and ADR-001..007 (aligned to AD-3/6/7/8/9/10) polished, status final 2026-09-20
